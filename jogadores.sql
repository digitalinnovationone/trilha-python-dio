CREATE TABLE jogadores(
    id INT INTO AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR (100),
    numerocamisa NUMERIC (10),
    salario NUMERIC (100),
    posicao VARCHAR (100),
    agente (100)
);